{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "cd \"/c/Users/guiha/OneDrive/Documentos/Projeto Claude\" && export PATH=\"$PATH:/c/Program Files/GitHub CLI\" && git add -A && git diff --cached --quiet || git commit -m \"Auto-update: $(date +%Y-%m-%dT%H:%M:%S)\" && git push 2>/dev/null || true",
            "timeout": 30,
            "statusMessage": "Sincronizando com GitHub..."
          }
        ]
      }
    ]
  }
}
